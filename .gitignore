node_modules/
.next/
.vercel/
.playwright-cli/
.autoresearch/

# OS / editor
.DS_Store
Thumbs.db

# logs
*.log
.reader.log

# env (NEVER commit secrets)
.env
.env.local
.env.*.local
.env.vercel.production
.env.vercel.check

# local data + backups
data/*.db
data/*.db.*
data/*.db-journal

# transient outputs
output/
tmp/
.cache/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# allow seed db to ship with deploy (Railway/Vercel build needs it)
!data/zero.db

# Local agent tooling (not for cloud)
.claude/
.codesight/
.wolf/
skills-lock.json
